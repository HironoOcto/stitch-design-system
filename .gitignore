# dependencies
node_modules/

# generated build artifacts (never committed — see ADR 0007; build-tokens.mjs default --out)
packages/tokens/dist/

# component library build output (vite lib mode)
packages/react/dist/

# demo 站构建产物（vite build，非 ci 步骤）
/dist/

# 构建期本地台账（从不入库、绝不删除、逐条清账；见 issue-management.zh-CN.md）
/预建笔记.md
/迁移笔记.md
