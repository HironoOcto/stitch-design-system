# 预建笔记（构建期临时台账 · 逐条清账，勿删文件）

> 无规格可依处的长相取舍、待人定夺项集中登记（同 demo-acceptance §5 Toggle 先例）。
> 本文件是仓库根的「越界内容」承接处之一：主题值 hex / 站名 / 长相由来写这里，**不进 `packages/react/src` 源码注释**（源码只留 stitch 语汇约束）。
> 生命周期：建 → 交人拍板 → **逐条清账（标定夺结果，勿删文件）**；构建期临时、不入库（git 从不追踪，同 `迁移笔记.md`）。

---

## LineChart（data-viz 族首件 · #2）—— ✅ 已拍板（#2 验收通过 2026-09-07）

**背景**：data-viz 族的第一个对外组件，`sites/*/rules.md` 无图表规格段、族内无同辈可对齐。以下长相取舍为**原创决定**（无规格可依），经人验收拍板，作为 data-viz 族雏形规格。

### 原创长相取舍 · 已定夺

| 取舍 | 现值 | 理由 | 定夺 |
|---|---|---|---|
| 线宽 | `strokeWidth={2}` | 细单线，贴「极简折线」气质 | ✅ 通过 |
| 数据点 | `dot={false}` + `activeDot={{ r: 4 }}` | 静态不铺点、hover 才现活跃点，减噪 | ✅ 通过 |
| 面积软填充 | `color-mix(in srgb, <系列色>, transparent 88%)` | 由系列色派生的极淡填充，随换肤跟随 | ✅ 通过 |
| 网格 | `CartesianGrid vertical={false}` | 只留横向基线，纵向留白 | ✅ 通过 |
| 入场动效 | `isAnimationActive={false}` | recharts 默认 JS 入场动画约 1.5s，接不到 `--stitch-motion-*` 且超出 design-rules 动效窗口（0.15–0.35s）→ 关掉。**若日后要动效**：需把时长/缓动接到 `--stitch-motion-*`（recharts 收 ms 数值、收不了 CSS var，故当前无法合规地开） | ✅ 通过 |
| 多系列区分通道 | 线型 `strokeDasharray` 按序循环 `['0','6 4','2 4','10 4 2 4']` + 末端标名（末点旁标系列名，文字走 `var(--stitch-text-primary)`） | 不只靠颜色分系列（守 WCAG 1.4.1）；主题线色偏淡时也能辨别。单系列恒实线、外观零变化 | ✅ 通过（方案 X） |

### steep 线色偏淡 —— ✅ 已定夺：接受为固有质感，非 bug

- **steep 的 `--stitch-cat-1: #fbe1d1`（blush-peach，近白）作单条折线色，在 canvas 白底上偏淡。**
  - 组件侧正确：系列色经换肤桥读 `--stitch-cat-*`，第一系列取 cat-1，无误。
  - **定夺（方案 X）：接受为 steep 单调主题的固有质感，不修**——颜色源一字不改（各站 `cat-*` 原样）。多系列的可分性已由**线型 + 末端标名**两条不依赖颜色的通道保证。
  - **未来项（方案 Y，尚未开）**：若要「每主题的线都醒目」，另立方案换线色源为 `accent`，不在 #2 内。
